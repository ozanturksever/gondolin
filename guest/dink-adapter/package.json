{
  "name": "@earendil-works/gondolin-dink-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dink edge sidecar for Gondolin VMs — bridges sandbox-agent to Dink mesh",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fatagnus/dink-sdk": "*",
    "node-pty": "^1.0.0"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  }
}
